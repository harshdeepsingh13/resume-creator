import React from 'react';
import './styles.scss';
import PropTypes from 'prop-types';

const Logo = props => {
return (
	<div className="logo-container">
		<img src="" alt="Resume Creator Logo"/>
	</div>
)
};

Logo.propTypes={};

export default Logo